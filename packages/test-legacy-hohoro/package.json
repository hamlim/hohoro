{
  "name": "test-legacy-hohoro",
  "version": "0.0.1",
  "module": "index.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "1.6.3",
    "@types/bun": "latest",
    "hohoro": "workspace:*",
    "react": "*",
    "@types/react": "19.2.2"
  },
  "scripts": {
    "dev": "node --watch-path=./src dev.mjs",
    "build": "hohoro-legacy",
    "type-check": "tsc -p ./tsconfig.json",
    "lint": "biome lint ./src/",
    "test": "bun test",
    "prepub": "bun run build && bun run type-check && bun run lint && bun run test",
    "pub": "npm publish --access public"
  }
}
